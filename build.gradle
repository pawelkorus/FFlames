apply plugin: 'java'

if (!hasProperty('mainClass')) {
    ext.mainClass = 'fflames.Application'
}

jar {
	manifest {
		attributes 'Main-Class': 'fflames.Application'
	}
}

task wrapper(type: Wrapper) {
	gradleVersion = '2.4'
}